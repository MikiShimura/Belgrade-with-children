<nav class="navbar sticky-top navbar-expand-lg navbar-dark bg-danger">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Belgrade with kids</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <div class="navbar-nav">
          <% if(currentUser && currentUser._id.equals("639f115406339b155eaeda04")) { %>
          <a class="nav-link" href="/new">New place</a>
          <% } %>
      </div>
      <div class="navbar-nav ms-auto">
        <% if(!currentUser){ %> 
          <a class="nav-link" href="/user/login">Login</a>
          <a class="nav-link" href="/user/register">Register user</a>
        <% }else{ %>
        <a class="nav-link" href="/user/logout">Logout</a>
        <% } %>
    </div>
    </div>
  </div>
</nav>
</nav>